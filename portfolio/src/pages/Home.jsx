import React from "react";
import Hero from "../components/Hero";

const Home = () => {
  return (
    <div>
      <Hero />
      {/* Add more sections below the hero as needed */}
    </div>
  );
};

export default Home;
